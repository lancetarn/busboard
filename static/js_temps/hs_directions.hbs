<select class="direction_select" name="direction">
    <option label="Select direction..." value="">
    {{#each this.available_directions}}
    <option label="{{text}}" value="{{value}}">
    {{/each}}
</select>
